const ClientConfig: ClientConfig = {
  appName: "Overreact demo",
  socketReconnectTimeout: 10000, // 10 seconds - show offline page if Socket.IO disconnected for this long
};

export default ClientConfig;
